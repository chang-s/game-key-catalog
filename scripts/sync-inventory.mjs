import fs from 'node:fs';import path from 'node:path';
const input=process.argv[2];if(!input)throw new Error('Usage: npm run sync -- path/to/export.csv');
const text=fs.readFileSync(input,'utf8');
const rows=[];let row=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(c==='"'&&quoted&&n==='"'){cell+='"';i++}else if(c==='"')quoted=!quoted;else if(c===','&&!quoted){row.push(cell);cell=''}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&n==='\n')i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell=''}else cell+=c}row.push(cell);if(row.some(Boolean))rows.push(row);
const headers=rows.shift();const required=['Item ID','Title','Offer Type','Genre','Image Filename','Primary Keys','Other Region Keys','Availability','Date Added','Active'];for(const h of required)if(!headers.includes(h))throw new Error(`Missing required column: ${h}`);
const platforms=['Xbox Play Anywhere','Xbox','Windows 10','Game Pass','Steam','Oculus','Battlenet','PlayStation 5','Mobile'];const get=(r,h)=>r[headers.indexOf(h)]?.trim()||'';const num=(r,h)=>Number(get(r,h)||0);
const parseOtherRegionInventory=details=>details.split(';').flatMap(group=>{const [platform,...regions]=group.split(':');return regions.join(':').split(',').map(value=>{const match=value.trim().match(/^(.*?)\s*[x×]\s*(\d+)$/i);return match?{platform:platform.trim(),region:match[1].trim(),quantity:Number(match[2])}:null}).filter(Boolean)});
const games=rows.filter(r=>get(r,'Item ID')).map(r=>{const otherRegionDetails=get(r,'Other Region Details');return {id:get(r,'Item ID').padStart(3,'0'),title:get(r,'Title'),offerType:get(r,'Offer Type'),genre:get(r,'Genre'),...(get(r,'Edition / Item')&&{edition:get(r,'Edition / Item')}),imageFilename:path.basename(get(r,'Image Filename')),platformQuantities:Object.fromEntries(platforms.map(p=>[p,num(r,p)]).filter(([,v])=>v>0)),primaryKeys:num(r,'Primary Keys'),otherRegionKeys:num(r,'Other Region Keys'),...(otherRegionDetails&&{otherRegionInventory:parseOtherRegionInventory(otherRegionDetails)}),availability:get(r,'Availability'),...(get(r,'Region / Restrictions')&&{regionRestrictions:get(r,'Region / Restrictions')}),...(get(r,'Notes')&&{notes:get(r,'Notes')}),dateAdded:get(r,'Date Added'),active:/^(true|yes|1)$/i.test(get(r,'Active'))}});
const serialized=JSON.stringify(games,null,2);const tokenPattern=/\b[A-Z0-9]{5}(?:-[A-Z0-9]{5}){4}\b/i;if(tokenPattern.test(serialized))throw new Error('Possible redemption token found. Output was not written.');for(const g of games){if(!/^\d{3}$/.test(g.id))throw new Error(`Invalid Item ID: ${g.id}`);if(g.imageFilename.includes('/')||g.imageFilename.includes('\\'))throw new Error(`Unsafe image filename for ${g.id}`);const regionalTotal=(g.otherRegionInventory||[]).reduce((total,item)=>total+item.quantity,0);if(regionalTotal!==g.otherRegionKeys)throw new Error(`Other Region Details total does not match Other Region Keys for ${g.id}`)}
fs.writeFileSync(new URL('../src/data/games.json',import.meta.url),serialized+'\n');console.log(`Wrote ${games.length} sanitized inventory items.`);
